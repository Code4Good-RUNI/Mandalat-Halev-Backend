# תכנית המשך וצעדים עיקריים

## בדיקות אינטגרציה
אכן, חשוב להריץ בדיקות מקצה לקצה (end-to-end) – לא רק בדיקות יחידה.
ממליץ להגדיר תרחישי בדיקה עיקריים (login, הרשמה, שליפת אירועים, הרשאות אדמין) ולוודא שכל הזרימות עובדות מול Salesforce אמיתי/סביבת טסט.

---

## מימוש שלושת סוגי הבקשות

### CRON
אפשר להתחיל עם `node-cron` או `agenda`, ולוודא שהמשימות לא רצות פעמיים (אם יש כמה אינסטנסים).

### POLLING
- `jsforce` אכן תומכת ב־PushTopic, אבל כדאי לבדוק גם את Event Monitoring של Salesforce (אם זמין).
- חשוב להגדיר מנגנון fallback (למשל, polling רגיל) אם PushTopic לא יציב.

### User Requests
- לכתוב את כל ה־endpoints לפי ה־High Level Design, כולל הרשאות.
- לוודא שכל אחד מהם מתועד.

---

## העדפת SMS לעומת מייל
ההערה נכונה – מייל הוא זול, פשוט, ומספיק לרוב המשתמשים.
אפשר להשאיר תמיכה ב־SMS רק למקרים חריגים/משתמשים ללא מייל.

